spring:
  application:
    name: discovery-service

server:
  port: 8761

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
