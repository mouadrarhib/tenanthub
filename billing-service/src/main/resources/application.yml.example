spring:
  application:
    name: billing-service
  datasource:
    url: jdbc:postgresql://localhost:5432/tenanthub_billing
    username: postgres
    password: changeme
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: billing-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: com.tenanthub.events
    producer:
      # Needed so KafkaErrorHandlingConfig's DeadLetterPublishingRecoverer can
      # serialize a failed event onto its "<topic>.DLT" topic.
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

# auth-service signs JWTs with jjwt + HS256 using this shared secret (not a real
# OIDC/OAuth2 issuer, so no issuer-uri/JWK discovery here) - must match auth-service's
# jwt.secret exactly. security/SecurityConfig builds a JwtDecoder from it.
jwt:
  secret: changeme-replace-with-a-strong-secret-key

server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
