# Template only - the real values below are placeholders, safe to commit.
# Never put real credentials in a file like this; instead do one of:
#   kubectl create secret generic tenanthub-secrets -n tenanthub \
#     --from-literal=POSTGRES_PASSWORD=<real value> \
#     --from-literal=JWT_SECRET=<real value>
# or run through a vault / sealed-secrets / your cloud provider's secret
# manager in real production. `data` values below are just
# base64 of the word "changeme" - Secrets are base64-*encoded*, not
# encrypted, which is exactly the limitation k8s-complete-guide.html (#9)
# calls out.
apiVersion: v1
kind: Secret
metadata:
  name: tenanthub-secrets
  namespace: tenanthub
type: Opaque
data:
  POSTGRES_PASSWORD: Y2hhbmdlbWU=
  JWT_SECRET: Y2hhbmdlbWU=
