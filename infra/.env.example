# Copy to infra/.env and fill in real values. infra/.env is gitignored -
# never commit it. docker compose loads it automatically because it sits
# next to docker-compose.yml.

POSTGRES_PASSWORD=changeme
JWT_SECRET=changeme-replace-with-a-strong-secret-key
